services:
  frontend:
    image: nginx:alpine
    volumes:
      - ./client/dist:/usr/share/nginx/html
    ports:
      - "81:80"
  backend:
    build: ./server
    ports:
      - "82:5000"
    env_file:
      - ./server/.env
    depends_on:
      - db
  db:
    image: mongo:latest
    ports:
      - "27017:27017"