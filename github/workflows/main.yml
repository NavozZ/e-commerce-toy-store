name: Toy Store CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install and Test Backend
        run: |
          cd server
          npm install
          # Note: In real CI you'd use a mock DB, for now we verify install